# Drop zone for native screen captures

Place operator-exported assets here when re-recording from the Mac:

| File | Purpose |
|------|---------|
| `console-loop.mp4` / `console-loop.gif` | Crop of command-console demo |
| `mcp-assure-check.mp4` / `.gif` | Live terminal of `mcp-assure check` |
| `leash-arm-deny.mp4` / `.gif` | ARM + password-manager deny |
| `session-bus-light.mp4` / `.gif` | Phone note → skill result |

Until present, `/media/` uses **CSS terminal films** fed by host receipts under `/media/receipts/` plus the live X embed for the console video.

Do not check in secrets, bus tokens, or Tailscale hostnames.
